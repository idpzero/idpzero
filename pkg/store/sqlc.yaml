version: "2"
sql:
  - engine: "sqlite"
    queries: "query.sql"
    schema: "./migrations"
    gen:
      go:
        package: "query"
        out: "query"
        emit_result_struct_pointers: true